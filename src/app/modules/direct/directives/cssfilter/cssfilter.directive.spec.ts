import { CSSFilterDirective } from './cssfilter.directive';

describe('CSSFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new CSSFilterDirective();
    expect(directive).toBeTruthy();
  });
});
